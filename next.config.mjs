/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['binaryinsurance.pt/admin/public'], // Ensure this matches your image URL
    },
  };
  
  export default nextConfig;
  